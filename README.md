# README: Отчет об исправлении ошибок

## Общие ошибки, исправленные во всех задачах

### 1. **Глобальное `using namespace std`**
   `//FIX_ME: глобальное using namespace std запрещено`
   `//using namespace std;`

### 2. **Транслит в названиях переменных и функций**
   `//FIX_ME: транслит в названиях переменных`
   `//int x;`
   `//Node* next;`
   `//int n, value, M;`

### 3. **Непонятные числа**
   `//FIX_ME: непонятные числа`
   `//return 1;`
   `//for (int i = 0; i < 5; ++i)`
   `//if (count % 2 == 1)`

### 4. **Отсутствие проверок**
   `//FIX_ME: отсутствовала проверка на успешное выделение памяти`
   `//Node* newNode = new Node(value);`
   
   `//FIX_ME: отсутствовала проверка на пустую структуру`
   `//if (top != nullptr)`

### 5. **Отсутствие деструктора**
   `//FIX_ME: отсутствовал деструктор`
   `//~Stack();`
   `//~LinkedList();`

### 6. **Отсутствие константности для методов, не изменяющих объект**
   `//FIX_ME: метод должен быть const`
   `//void print()`
   `//void Display()`

### 7. **Отсутствие функции очистки ввода при ошибках**
   `//FIX_ME: добавлена функция для очистки ввода`
   `//void ClearInput()`

---

## Задача 1: Стек (Stack)

### Критические ошибки:

#### 1. **Непонятные имена структуры и полей**
   `//FIX_ME: непонятное имя поля`
   `//int x;`
   
   `//FIX_ME: поле должно называться Next`
   `//Node* next;`

#### 2. **Непонятные имена методов**
   `//FIX_ME: транслит в названиях методов`
   `//void push(int d);`
   `//void show();`
   
   `//FIX_ME: имя метода должно начинаться с большой буквы`
   `//bool isEmpty();`

#### 3. **Неправильный возврат значения**
   `//FIX_ME: неправильный возврат числа 1 при ошибке`
   `//int getTopValue() { if (top != nullptr) { return top->x; } return 1; }`

#### 4. **Отсутствие проверки выделения памяти**
   `//FIX_ME: отсутствовала проверка на успешное выделение памяти`
   `//Node* newNode = new Node(Value);`

#### 5. **Отсутствие деструктора**
   `//FIX_ME: отсутствовал деструктор`
   `//~Stack();`

#### 6. **Отсутствие метода удаления**
   `//FIX_ME: отсутствовал метод удаления`
   `//bool Pop();`

---

## Задача 2: Очередь (TQueue)

### Критические ошибки:

#### 1. **Непонятные имена структур**
   `//FIX_ME: тип узла должен называться TNode по заданию`
   `//struct Node`
   
   `//FIX_ME: тип должен называться TQueue по заданию`
   `//struct Queue`

#### 2. **Непонятные имена полей**
   `//FIX_ME: поле должно называться Data по заданию`
   `//int x;`
   
   `//FIX_ME: поля должны называться Head и Tail по заданию`
   `//PNode front;`
   `//PNode last;`

#### 4. **Отсутствие проверки выделения памяти**
   `//FIX_ME: отсутствовала проверка на успешное выделение памяти`
   `//TNode* newNode = new TNode;`

#### 5. **Непонятное число 5**
   `//FIX_ME: магическое число 5`
   `//for (int i = 0; i < 5; ++i)`

#### 6. **Неправильный вывод адресов при пустой очереди**
   `//FIX_ME: если очередь пуста, адреса должны быть равны nullptr`
   `//cout << "Адрес начала: " << Q.front << endl;`

#### 7. **Отсутствие проверки на пустую очередь**
   `//FIX_ME: отсутствовала проверка на пустую очередь`
   `//if (Q.front != nullptr)`

---

## Задача 3: Односвязный линейный список

### Критические ошибки:

#### 1. **Имена полей и методов должны начинаться с большой буквыe**
   `//FIX_ME: поле должно называться Data`
   `//int data;`
   
   `//FIX_ME: поле должно называться Next`
   `//Node* next;`

   `//FIX_ME: непонятное имя поля`
   `//Node* head;`

   `//FIX_ME: непонятное имя метода`
   `//void add(int value)`
   
   `//FIX_ME: непонятное имя метода`
   `//void print()`
   
   `//FIX_ME: непонятное имя метода`
   `//void clearList()`
   
   `//FIX_ME: непонятное имя метода`
   `//Node* getLastNode()`

#### 4. **Отсутствие проверки выделения памяти**
   `//FIX_ME: отсутствовала проверка на успешное выделение памяти`
   `//Node* newNode = new Node(value);`

#### 5. **Отсутствие проверки на пустой список**
   `//FIX_ME: отсутствовала проверка на пустой список`
   `//Node* current = head;`


#### 7. **Отсутствие обработки нечетного количества элементов**
   `//FIX_ME: не обрабатывается случай нечетного количества элементов`
   `//while (current != nullptr && current->next != nullptr)`

#### 8. **Отсутствие деструктора**
   `//FIX_ME: отсутствовал деструктор`
   `//~LinkedList();`

#### 9. **Отсутствие константности для методов, не изменяющих объект**
   `//FIX_ME: метод должен быть const`
   `//void print()`
   `//Node* getLastNode()`

#### 10. **Отсутствие проверки ввода**
    `//FIX_ME: отсутствовала проверка ввода`
    `//cin >> n;`
    `//cin >> value;`
    `//cin >> M;`

#### 11. **Отсутствие функции очистки ввода**
    `//FIX_ME: добавлена функция для очистки ввода`
    `//void ClearInput()`


